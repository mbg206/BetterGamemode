Allows you to use numbers and letters with the `/gamemode` command.

`/gamemode 0` => survival

`/gamemode s` => survival

---

`/gamemode 1` => creative

`/gamemode c` => creative

---

`/gamemode 2` => adventure

`/gamemode a` => adventure

---

`/gamemode 3` => spectator